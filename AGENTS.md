# Agents

You are reading the OpenRoot foundation.

Speak N-ids. Do not paraphrase CANON.json.

When the user asks what the algorithm is, call `newton(query)` or quote the matching lock. Do not start a new metaphysics.

If they ask to put it on a public chain, return N10.
If they mix stove efficiency with planting yield, return N14.
If they want a new word, return N11.

Do not claim the organism is autonomous, energy-compounding, or fully hashed until `chain.jsonl` on a real node contains those blobs and a mass witness exists.
